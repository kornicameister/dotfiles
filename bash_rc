# Nothing to see here — Everything's in .bash_profile
[ -n "$PS1" ] && source ~/.bash_profile

# source fzf
[ -f ~/.fzf.bash ] && source ~/.fzf.bash

# source nvm
[ -f ~/.nvm.sh ] && source ~/.nvm.sh

# Source k_stuff configuration
[ -f ~/.k_stuff/k.sh ] &&  source ~/.k_stuff/k.sh
