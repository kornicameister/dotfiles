#!/bin/zsh


# settings for prezto imported stuff
ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE='fg=6'
ZSH_AUTOSUGGEST_STRATEGY=(history completion)

HISTORY_SUBSTRING_SEARCH_HIGHLIGHT_FOUND='bg=cyan,fg=white,bold'
HISTORY_SUBSTRING_SEARCH_HIGHLIGHT_NOT_FOUND='bg=red,fg=white,bold'
HISTORY_SUBSTRING_SEARCH_GLOBBING_FLAGS='i'
HISTORY_SUBSTRING_SEARCH_ENSURE_UNIQUE='yes'

zstyle ':prezto:*:*' color 'yes'
zstyle ':prezto:*:*' case-sensitive 'false'
zstyle ':prezto:module:utility' correct 'yes'
zstyle ':prezto:module:utility' safe-ops 'yes'
zstyle ':prezto:module:utility:ls' dirs-first 'yes'
zstyle ':prezto:module:ssh:load' identities 'github'
zstyle ':prezto:module:completion:*:hosts' etc-host-ignores '0.0.0.0' '127.0.0.1'
zstyle ':prezto:module:editor' key-bindings 'emacs'
zstyle ':prezto:module:editor' dot-expansion 'yes'
zstyle ':prezto:module:editor' ps-context 'yes'
zstyle ':prezto:module:autosuggestions:color' found 'fg=6'
zstyle ':prezto:module:terminal' auto-title 'yes'

# loading *envs
[ $commands[pyenv] ] && {
    eval "$(pyenv init - --no-rehash zsh)"
    eval "$(pyenv virtualenv-init -)"
}
[ $commands[nodenv] ] && {
    eval "$(nodenv init - --no-rehash zsh)"
}
[ $commands[goenv] ] && {
    eval "$(goenv init - --no-rehash zsh)"
    path=(
        $GOROOT/bin
        $GOPATH/bin
        $path
    )
    export GOBIN="$GOPATH/bin";
}
[ $commands[kubectl] ] && source <(kubectl completion zsh)

source ~/.zplugin/zplugin.zsh

# zplugin...plugins :)
zplugin light zdharma/z-p-submods

# prompt
zplugin ice \
    pick'async.zsh' \
    src'pure.zsh' \
    atload'source ~/.dotfiles/zsh/prompt.zsh'
zplugin light sindresorhus/pure

# plugins of all sorts
zplugin load zdharma/zsh-unique-id
zplugin ice wait lucid ; zplugin light zdharma/z-p-submods
zplugin ice wait lucid ; zplugin light sobolevn/wakatime-zsh-plugin
zplugin ice wait lucid ; zplugin light RobSis/zsh-reentry-hook
zplugin ice wait'1' lucid ; zplugin light hlissner/zsh-autopair
zplugin ice wait'1' lucid ; zplugin light djui/alias-tips
zplugin ice wait'1' lucid ; zplugin load voronkovich/gitignore.plugin.zsh
zplugin ice wait'1' lucid pick"h.sh" ; zplugin light paoloantinori/hhighlighter
zplugin ice wait'1' lucid ; zplugin load 'wfxr/forgit'

# jq
zplugin ice from'gh-r' \
    as'command' \
    mv'jq* -> jq' \
    bpick'*linux*'
zplugin light stedolan/jq

# diff-s-fancy
zplugin ice \
    lucid \
    wait'1' \
    as'program' \
    pick'bin/git-dsf'
zplugin light zdharma/zsh-diff-so-fancy

# dircolors
zplugin ice wait"0c" lucid reset \
    atclone"local P=${${(M)OSTYPE:#*darwin*}:+g}
            \${P}sed -i \
            '/DIR/c\DIR 38;5;63;1' LS_COLORS; \
            \${P}dircolors -b LS_COLORS > c.zsh" \
    atpull'%atclone ; !git reset --hard' pick"c.zsh" nocompile'!' \
    atload'zstyle ":completion:*" list-colors “${(s.:.)LS_COLORS}”'
zplugin light trapd00r/LS_COLORS

# replacement for ls
zplugin ice \
    wait'2' \
    lucid \
    from'gh-r' \
    as'program' \
    mv'exa* -> exa'
zplugin light ogham/exa

# bat
zplugin ice \
    wait'2' \
    lucid \
    as'command' \
    from'gh-r' \
    mv'bat* -> bat' \
    pick'bat/bat'
zplugin light sharkdp/bat

# direnv
zplugin ice \
    as'program' \
    atclone'./direnv hook zsh > zhook.zsh' \
    atpull'%atclone'\
    src'zhook.zsh' \
    make'!'
zplugin light direnv/direnv

# git extra stuff
zplugin ice \
    wait \
    lucid \
    as'program' \
    pick'$ZPFX/bin/git-*' \
    make'PREFIX=$ZPFX' \
    nocompile
zplugin light tj/git-extras

zplugin ice \
    svn \
    wait \
    lucid \
    submods'zsh-users/zsh-completions -> external'
zplugin snippet PZT::modules/completion

zplugin ice \
    wait \
    blockf \
    lucid \
    atclone'./zplug.zsh'
zplugin light g-plane/zsh-yarn-autocompletions

zplugin ice \
    wait \
    lucid
zplugin light lukechilds/zsh-better-npm-completion

zplugin ice \
    wait \
    lucid
zplugin light srijanshetty/zsh-pandoc-completion

zplugin ice \
    wait \
    lucid \
    atinit'ZPLGM[COMPINIT_OPTS]=-C; zpcompinit; zpcdreplay'
zplugin load zdharma/fast-syntax-highlighting

zplugin ice \
    svn \
    wait \
    lucid \
    submods'zsh-users/zsh-autosuggestions -> external' \
    atload'!_zsh_autosuggest_start' \
    wrap-track'_zsh_autosuggest_start'
zplugin snippet PZT::modules/autosuggestions

zplugin ice as'completion' ; zplugin snippet https://raw.githubusercontent.com/tj/git-extras/master/etc/git-extras-completion.zsh
zplugin ice as'completion' ; zplugin snippet https://github.com/docker/cli/blob/master/contrib/completion/zsh/_docker

zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/environment
zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/terminal
zplugin ice wait'1' svn silent submods'clvv/fasd -> external' ; zplugin snippet PZT::modules/fasd
zplugin ice wait'1' svn silent submods'zsh-users/zsh-history-substring-search -> external' ; zplugin snippet PZT::modules/history-substring-search
zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/editor
zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/history
zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/directory
zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/utility
zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/helper
zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/gpg
zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/ssh
zplugin ice wait'1' svn silent ; zplugin snippet PZT::modules/git
zplugin ice wait'1' svn silent pick'' ; zplugin snippet PZT::modules/archive

source ~/.fzf.zsh
source ~/.fzf.settings
source ~/.npmrc.sh

[[ -f ~/.zshrc.local ]] && source ~/.zshrc.local
