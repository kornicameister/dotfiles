---
- defaults:
    link:
      relink: true

- clean: ["~"]

- shell:
    - command: mkdir -p $HOME/.local/bin
      description: Create directory for local binaries
    - command: ./pre-install

- link:
    ~/.dotfiles: ""

    # vim stuff
    ~/.vim: vim
    ~/.vimrc: vimrc

    # neovim stuff
    ~/.config/nvim: nvim

    # git stuff
    ~/.gitignore: gitignore
    ~/.gitconfig: gitconfig
    ~/.git_template: git_template

    # zsh stuff
    ~/.zshrc: zsh/zshrc
    ~/.zpreztorc: zsh/zpreztorc
    ~/.zlogin: zsh/zlogin
    ~/.zprofile: zsh/zprofile
    ~/.zshenv: zsh/zshenv
    ~/.zhooks: zsh/hooks.zsh
    ~/.zprezto: dependencies/prezto
    ~/.zgen: dependencies/zgen

    # fzf
    ~/.fzf: dependencies/fzf
    ~/.fzf.settings: fzf.settings

    # node
    ~/.nodenv: dependencies/nodenv
    ~/.nodenv/version: node-system-version
    ~/.npmrc.sh: npmrc.sh

    # pyenv
    ~/.pyenv: dependencies/pyenv
    ~/.pyenv.sh: pyenv.sh

    # ctags
    ~/.ctags: ctags

    # t - todos
    ~/.t.sh: t.sh
    ~/.local/bin/t: dependencies/t/t.py

    # custom application configuration
    ~/.config/terminator: config/terminator

    # custom binaries
    ~/.local/bin:
      glob: true
      path: bin/*

- shell:
    - command: ./post-install
